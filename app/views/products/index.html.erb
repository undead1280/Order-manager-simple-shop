 <% @products.each do |product| %>
  <%= form_for(@order_item) do |f| %>
    <%= product.name %><br><%= number_to_currency product.price %><br>
    <%= f.hidden_field :product_id, value: product.id %>
    <%= f.number_field :quantity %>
    <%= f.submit "Add to cart" %>
  <% end %>
<% end %>
